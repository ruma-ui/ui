name: CI

on:
  push:
    branches:
      - dev
      - alpha
      - beta
      - rc
      - main
  pull_request:
    branches:
      - dev
      - alpha
      - beta
      - rc
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  # Check what files changed to conditionally run jobs
  changes:
    runs-on: ubuntu-latest
    outputs:
      src: ${{ steps.changes.outputs.src }}
      config: ${{ steps.changes.outputs.config }}
      workflows: ${{ steps.changes.outputs.workflows }}
      docs: ${{ steps.changes.outputs.docs }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check changed files
        id: changes
        uses: dorny/paths-filter@v2
        with:
          filters: |
            src:
              - 'ui/src/**'
              - 'demo/src/**'
              - 'www/src/**'
            config:
              - 'package.json'
              - 'pnpm-lock.yaml'
              - '**/package.json'
              - 'nx.json'
              - 'tsconfig*.json'
            workflows:
              - '.github/workflows/**'
            docs:
              - 'docs/**'
              - '*.md'

  # Initial setup and dependency installation
  setup:
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.config == 'true' || needs.changes.outputs.workflows == 'true'
    outputs:
      cache-hit: ${{ steps.cache-deps.outputs.cache-hit }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Enhanced caching strategy
      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Cache Nx
        uses: actions/cache@v4
        with:
          path: .nx/cache
          key: ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-
            ${{ runner.os }}-nx-

      - uses: pnpm/action-setup@v4
        with:
          version: 9.8.0
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      # Cache dependencies with retry
      - name: Cache dependencies
        id: cache-deps
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/pnpm-lock.yaml') }}

      - name: Install dependencies with retry
        if: steps.cache-deps.outputs.cache-hit != 'true'
        uses: nick-fields/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          command: pnpm install --frozen-lockfile

  # Code quality and linting
  quality:
    runs-on: ubuntu-latest
    needs: [changes, setup]
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.config == 'true' || needs.changes.outputs.workflows == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          version: 9.8.0
          run_install: false

      # Restore caches
      - name: Restore pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Restore Nx cache
        uses: actions/cache@v4
        with:
          path: .nx/cache
          key: ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-
            ${{ runner.os }}-nx-

      - name: Restore dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/pnpm-lock.yaml') }}

      # Enable Nx Cloud CI run (without distribution due to agent requirement)
      - name: Start Nx Cloud CI run
        run: pnpm dlx nx start-ci-run

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies (if cache miss)
        if: needs.setup.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      # Security scanning
      - name: Run security audit
        run: pnpm audit --audit-level high
        continue-on-error: true

      # Dependency scanning for PRs
      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        if: github.event_name == 'pull_request'

      # Run validation pipeline
      - run: pnpm run validate

      # Check bundle size
      - run: pnpm run size

      # Build docs
      - run: pnpm run docs:build

      # Nx Cloud recommends fixes for failures to help you get CI green faster
      - run: pnpm dlx nx fix-ci
        if: always()

  # Testing
  test:
    runs-on: ubuntu-latest
    needs: [changes, setup]
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.config == 'true' || needs.changes.outputs.workflows == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 9.8.0
          run_install: false

      # Restore caches
      - name: Restore dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/pnpm-lock.yaml') }}

      - name: Restore Nx cache
        uses: actions/cache@v4
        with:
          path: .nx/cache
          key: ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-
            ${{ runner.os }}-nx-

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies (if cache miss)
        if: needs.setup.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      - run: pnpm run test

  # Build
  build:
    runs-on: ubuntu-latest
    needs: [changes, setup]
    if: needs.changes.outputs.src == 'true' || needs.changes.outputs.config == 'true' || needs.changes.outputs.workflows == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 9.8.0
          run_install: false

      # Restore caches
      - name: Restore dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/pnpm-lock.yaml') }}

      - name: Restore Nx cache
        uses: actions/cache@v4
        with:
          path: .nx/cache
          key: ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-nx-${{ hashFiles('**/package.json') }}-
            ${{ runner.os }}-nx-

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies (if cache miss)
        if: needs.setup.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      - run: pnpm run build

  # Notify on failures
  notify-failure:
    needs: [quality, test, build]
    runs-on: ubuntu-latest
    if: always() && (needs.quality.result == 'failure' || needs.test.result == 'failure' || needs.build.result == 'failure')
    permissions:
      issues: write
    steps:
      - name: Create failure issue
        uses: actions/github-script@v7
        with:
          script: |
            const runUrl = `${context.payload.repository.html_url}/actions/runs/${context.runId}`;
            const branch = '${{ github.ref_name }}';

            // Get job statuses from workflow context
            const qualityResult = '${{ needs.quality.result }}';
            const testResult = '${{ needs.test.result }}';
            const buildResult = '${{ needs.build.result }}';

            // Build the failed jobs list
            let failedJobs = '';
            if (qualityResult === 'failure') failedJobs += '- Quality checks\n';
            if (testResult === 'failure') failedJobs += '- Tests\n';
            if (buildResult === 'failure') failedJobs += '- Build\n';

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🚨 CI Failed on ${branch}`,
              body: `The CI workflow failed on branch \`${branch}\`.\n\n**Workflow Run:** ${runUrl}\n\n**Failed Jobs:**\n${failedJobs}\nPlease check the logs for details.`,
              labels: ['bug', 'ci', `branch:${branch}`]
            });
