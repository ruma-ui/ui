# NPM registry authentication for local development only
# Uncomment the line below and set NPM_TOKEN environment variable for local development
# This is commented out to prevent CI/CD warnings when NPM_TOKEN is not available
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
